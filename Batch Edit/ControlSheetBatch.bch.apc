{Application 'CONTROLSHEETBATCH' logic file generated by CSPro}
PROC GLOBAL

PROC LISTING_FF

PROC IDS0_EDT
postproc
	if LSTROUND = notappl and 
	LSTED = notappl and
	LSTPARISH = notappl and
	LSTSTRATUM = notappl and
	LSTBUILDING = notappl and
	LSTSAMPLE = notappl then
		skip case;		//Delete blank cases
	endif;
	
//-----------------------------------------------------------------
PROC LSTDELETE_FLAG
if $ = 1 then		//if delete flag is set
	skip case;		//skip (delete) this case
endif;
